// Game-specific layer declarations
declare enum GameLayers {
  npc = 'npc',
  player = 'player'
}

// Game-specific animation declarations
declare enum GameAnimations {
  jump = 'jump',
  kick = 'kick'
}
